version: '3.8'

services:
  api:
    build: .
    container_name: apiglobalbox
    ports:
      - "3000:3000"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: always
